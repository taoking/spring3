apiVersion: v1
kind: Secret
metadata:
  name: spring3-runtime-secret
  namespace: spring3
  labels:
    app.kubernetes.io/part-of: spring3-learning
type: Opaque
stringData:
  # Example only. Replace with a real DSN in your own cluster and never commit real secrets.
  SENTRY_DSN: ""

