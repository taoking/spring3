demo:
  security:
    mode: jwt
    jwt:
      secret: ${DEMO_SECURITY_JWT_SECRET:spring3-local-dev-secret-key-32-bytes-minimum}
      issuer-uri: ${DEMO_SECURITY_JWT_ISSUER_URI:}
      jwk-set-uri: ${DEMO_SECURITY_JWT_JWK_SET_URI:}
